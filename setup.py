# -*- coding: utf-8 -*-
#
# Sinedra: Setup
#
# Author: Timu Eren <timu@sinelist.com>
#

from setuptools import setup, find_packages

setup(name="Validpie",
      version='0.1b',
      description="Validation library",
      url="http://code.google.com/p/validpie/",
      packages=find_packages(),
      include_package_data=True,
      author="Timu Eren",
      author_email="timu@sinelist.com",
      license="GPL v3",
      keywords="validation",
      install_requires=[],
      zip_safe=False,
      tests_require=[],
      dependency_links=[])
